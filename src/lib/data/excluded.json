{
	"$schema": "./excluded.schema.json",
	"_note": "Live site-exclusion list. `slugs` gates the public site by manifest slug (after a repo has been fingerprinted). Written by `drift hide <slug>`. Repository folder names excluded from the directory scan have moved to drift.config.ts → excludedRepoNames (paired to scanRoot); see drift.config.example.ts for the template.",
	"slugs": ["kamino"]
}
