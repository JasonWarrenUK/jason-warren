node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Source repo local paths (per-machine; see src/lib/data/sources.json for the committed snapshot)
src/lib/data/sources.local.json
# Per-machine HEAD-SHA fingerprint cache (speeds up repeated drift checks)
src/lib/data/.drift-cache.json
# Per-machine Drift engine config (see drift.config.example.ts for the template)
/drift.config.ts
/.codex-pets/tinker
.playwright-mcp/
