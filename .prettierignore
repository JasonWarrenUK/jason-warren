.svelte-kit
build
node_modules
package-lock.json
bun.lock

# Hand-authored; prettier misreads the PR-template separators as headings.
CLAUDE.md

# Owned by the roadmap CLI, which enforces its own canonical form (tabs,
# trailing newline) and refuses to recompute non-canonical files.
.claude/roadmaps.json

# Generated by the roadmap CLI (roadmap.py render) with its own canonical
# form (tabs, single-line JSON payload); do not let Prettier reflow it.
docs/artefacts/roadmap-*.html
